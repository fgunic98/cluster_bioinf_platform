global:
  scrape_interval: 5s

scrape_configs:

  - job_name: "spring-api"
    metrics_path: "/actuator/prometheus"
    static_configs:
      - targets: ["api:8080"]

  - job_name: "postgres"
    static_configs:
      - targets: ["postgres-exporter:9187"]
