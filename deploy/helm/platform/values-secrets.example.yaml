# Copy this file to values-secrets.yaml and fill in real values.
# values-secrets.yaml is gitignored — do not commit it.                                                                                                                                
#
# Pass it at install time alongside the main values file:                                                                                                                              
#   helm upgrade --install platform deploy/helm/platform \                                                                                                                             
#     -n platform --create-namespace \
#     -f deploy/helm/platform/values.yaml \                                                                                                                                            
#     -f deploy/helm/platform/values-secrets.yaml

postgres:
  auth:
    username: admin
    password: changeme